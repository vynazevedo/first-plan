# {{ project_name }} - Cursor Rules

Generated by first-plan-engine generate --tool cursor. Re-run to update.

{% if not has_ir %}
No `.first-plan/` IR found. Run `first-plan-engine init` first.
{% else %}

## Context

This codebase has compiled context at `.first-plan/`. Read those files for detailed guidance.

## Stacks
{% for stack in stacks %}
{{ stack }}
{% endfor %}

## Rules to follow

1. Follow existing conventions in `.first-plan/02-conventions/`. Do not invent new patterns.
2. Before implementing, check `.first-plan/03-reuse-index/` for existing solutions.
3. Respect quality signals in `.first-plan/11-quality/`. Low-coverage files are risky.
4. Honor contracts in `.first-plan/12-contracts/`. Do not break OpenAPI/Protobuf/GraphQL specs.
5. Avoid deprecated patterns from `.first-plan/13-evolution/`. Team already replaced them.
6. Consider runtime state from `.first-plan/14-runtime/`. Is code released or unreleased?

## Feature awareness
{% if features_summary %}
{{ features_summary }}
{% endif %}

## Quality state
{% if quality_summary %}
{{ quality_summary }}
{% endif %}

## Contracts
{% if contracts_summary %}
{{ contracts_summary }}
{% endif %}

## Refresh
`first-plan-engine generate --tool cursor`

{% endif %}
